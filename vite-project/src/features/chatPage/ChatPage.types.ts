/** Props passed to the pure chat layout view. */
export type ChatPageViewProps = {
  userName: string | undefined;
};

/** Props for the left pane (user header + conversation list). */
export type ChatSidebarProps = {
  userName: string | undefined;
};
