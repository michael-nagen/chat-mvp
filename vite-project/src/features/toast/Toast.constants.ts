/** How long (ms) the toast stays visible before auto-dismissing. */
export const AUTO_DISMISS_MS = 3000;
/** z-index ensures the toast floats above all other content. */
export const TOAST_Z_INDEX = 1000;
